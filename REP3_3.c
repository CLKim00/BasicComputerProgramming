#include <stdio.h>
#include <math.h>

int main(void)
{
    double a, b, c, dis;

    printf("계수를 입력하시오: ");  scanf("%lf", &a);
    printf("계수를 입력하시오: ");  scanf("%lf", &b);
    printf("계수를 입력하시오: ");  scanf("%lf", &c);

}